{% extends 'admin/baseAdmin.html.twig' %}

{% block body %}
    <h1>Agendas list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Lieu</th>
                <th>Time</th>
                <th>Spectacle</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for agenda in agendas %}
            <tr>
                <td><a href="{{ path('admin_agenda_show', { 'id': agenda.id }) }}">{{ agenda.id }}</a></td>
                <td>{% if agenda.date %}{{ agenda.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ agenda.lieu }}</td>
                <td>{% if agenda.time %}{{ agenda.time|date('H:i:s') }}{% endif %}</td>
                <td>{{ agenda.spectacle }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_agenda_show', { 'id': agenda.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_agenda_edit', { 'id': agenda.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_agenda_new') }}">Create a new agenda</a>
        </li>
    </ul>
{% endblock %}
